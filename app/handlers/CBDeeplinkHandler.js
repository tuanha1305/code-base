import CBConstant from 'constants/CBConstant';

export default class CBDeeplinkHandler {

    static handleUrl(url, uriPrefix = CBConstant.URL_PATTERN) {

    }

    static handleDynamicUrl(url, uriPrefix = CBConstant.DYNAMIC_URL_PATTERN) {

    }
}
